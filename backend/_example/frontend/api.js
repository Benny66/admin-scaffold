// 这是「黄金路径」唯一范例的前端 API 定义片段模板。
// 生成器 gen-module.sh 将本文件内容追加到 frontend/src/api/index.js 末尾。
//
// 示例片段（生成后自动替换 Example/example）：
//
//   // ==================== 示例管理 ====================
//   export const getExampleList = (params) => request.get('/examples', { params })
//   export const getExample = (id) => request.get(`/examples/${id}`)
//   export const createExample = (data) => request.post('/examples', data)
//   export const updateExample = (id, data) => request.put(`/examples/${id}`, data)
//   export const deleteExample = (id) => request.delete(`/examples/${id}`)
