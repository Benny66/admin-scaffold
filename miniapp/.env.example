# miniapp 端 dev 环境配置示例
# 复制本文件为 .env 并按需修改（.env 已被 .gitignore 忽略，不提交）
#
# VITE_API_BASE：后端地址（含协议 + 主机 + 端口）。
# 小程序请求不走 vite dev proxy（受限于微信小程序网络请求白名单机制），
# 业务代码 MUST 通过 import.meta.env.VITE_API_BASE 读取后端地址。
#
# dev 阶段填本地 http://localhost:8080（微信开发者工具须在「详情 → 本地设置」
# 勾选「不校验合法域名」）；生产部署填真实 https 域名（须在小程序后台配置 request 合法域名）。
VITE_API_BASE=http://localhost:8080
