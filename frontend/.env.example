# 前端 dev 环境配置示例
# 复制本文件为 .env 并按需修改（.env 已被 .gitignore 忽略，不提交）
#
# VITE_API_BASE：后端地址（含协议 + 主机 + 端口），用于 vite dev proxy 转发 /api 与 /static。
# 缺省（不配置）时回退 http://localhost:8080。
VITE_API_BASE=http://localhost:8080
